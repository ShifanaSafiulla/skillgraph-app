services:
  - type: web
    name: skillgraph-app
    runtime: node
    buildCommand: npm install && cd client && npm install && npm run build
    startCommand: node server/server.js
    envVars:
      - key: MONGODB_URI
        value: mongodb+srv://2403717673722041_db_user:Shif%40123@cluster0.d4vg1qc.mongodb.net/skillgraph?retryWrites=true&w=majority&appName=Cluster0
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 5000
