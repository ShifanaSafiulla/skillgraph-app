{
  "name": "skillgraph-app",
  "version": "1.0.0",
  "description": "Functional SkillGraph Application (MERN - React, Node, Express, MongoDB)",
  "main": "server/server.js",
  "type": "module",
  "scripts": {
    "start": "cmd /c \"cd client && npm run build\" && node server/server.js",
    "server": "node server/server.js",
    "seed": "node server/seed.js",
    "client": "cmd /c \"cd client && npm run dev\"",
    "build": "cmd /c \"cd client && npm run build\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  }
}
